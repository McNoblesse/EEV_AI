services:
  api:
    container_name: eeV-AI
    build: .
    ports:
      - "8930:8000"
    env_file:
      - .env
    environment:
      - PYTHONPATH=/app
    volumes:
      - .:/app
    restart: unless-stopped